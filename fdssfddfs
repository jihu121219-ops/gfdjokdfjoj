-- [[ Rivals 전용 하이엔드 + 강력 사일런트 에임 + 고급 ESP - GPDMD KOREA ]] --

local g = game
local plrs = g:GetService("Players")
local rs = g:GetService("RunService")
local lp = plrs.LocalPlayer
local uis = game:GetService("UserInputService")
local mouse = lp:GetMouse()
local rep = game:GetService("ReplicatedStorage") -- 추가

-- [ 라이브러리 로드 ]
local itemLib = require(rep.Modules.ItemLibrary)
local cosmeticLib = require(rep.Modules.CosmeticLibrary)
local animLib = require(rep.Modules.AnimationLibrary)
local util = require(rep.Modules.Utility) -- 유틸리티 모듈 로드
local Fluent = loadstring(game:HttpGet("https://raw.githubusercontent.com/jihu121219-ops/gfdjokdfjoj/refs/heads/main/fdssfddfs"))()

-- [ UI 창 생성 ]
local Window = Fluent:CreateWindow({
    Title = "헤응이 만든 슬링샷 핵",
    SubTitle = "개발자 헤응",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 500),
    Acrylic = false, 
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.RightShift
})

local Tabs = {
    Combat = Window:AddTab({ Title = "월뱅,사일런트", Icon = "crosshair" }),
    Visuals = Window:AddTab({ Title = "ESP관련", Icon = "eye" }),
    Movement = Window:AddTab({ Title = "이동/디싱크", Icon = "move" }),
    Settings = Window:AddTab({ Title = "스나 스킨체인지", Icon = "settings" })
}

-- [ 변수 설정 ]
local idk = false -- 월뱅 (기존 투사체 로직용)
local SilentAimEnabled = false -- 사일런트 에임 토글
local FlyEnabled = false
local FlySpeed = 50
local SpinEnabled = false
local SpinSpeed = 50
local AimbotEnabled = false
local AimbotSmoothing = 1
local FOVRadius = 200

-- ESP 설정
local ESPEnabled = false
local ESP_Boxes = true
local ESP_Names = true

-- [[ 1. 신규 사일런트 에임 핵심 로직 (Utility Hook) ]] --
local ents = {}
local function scan()
    ents = {}
    for _, v in pairs(workspace:GetChildren()) do
        if v:FindFirstChildOfClass("Humanoid") then
            table.insert(ents, v)
        elseif v.Name == "HurtEffect" then
            for _, hv in pairs(v:GetChildren()) do
                if hv.ClassName ~= "Highlight" then
                    table.insert(ents, hv)
                end
            end
        end
    end
end

local function getClosestToCenter()
    local char = lp.Character
    if not char then return end
    
    scan()
    local best, dist = nil, FOVRadius -- FOV 설정값 사용
    local cam = workspace.CurrentCamera
    local scr = cam.ViewportSize / 2
    
    for _, v in pairs(ents) do
        if v == char or v.Name == lp.Name then continue end
        local hrp = v:FindFirstChild("HumanoidRootPart")
        if not hrp then continue end
        
        local pos, vis = cam:WorldToViewportPoint(hrp.Position)
        if not vis then continue end
        
        local d = (Vector2.new(pos.X, pos.Y) - scr).Magnitude
        if d < dist then
            best = v
            dist = d
        end
    end
    return best
end

-- Utility.Raycast 후킹 (기존 사일런트 에임 대체)
local old_ray = util.Raycast
util.Raycast = function(s, o, d, len, f, ft, viz)
    if SilentAimEnabled and len == 999 and len > 100 and f then
        local tgt = getClosestToCenter()
        if tgt and tgt:FindFirstChild("Head") then
            local hitpos = tgt.Head.Position
            return {
                Position = hitpos,
                Distance = (hitpos - o).Magnitude,
                Instance = tgt.Head,
                Material = tgt.Head.Material,
                Normal = Vector3.yAxis
            }
        end
    end
    return old_ray(s, o, d, len, f, ft, viz)
end

-- [[ 2. ESP 로직 (고급형) ]] --
local function CreateESP(plr)
    local Box = Drawing.new("Square")
    Box.Thickness = 1
    Box.Filled = false
    local Name = Drawing.new("Text")
    Name.Size = 14
    Name.Center = true
    Name.Outline = true

    rs.RenderStepped:Connect(function()
        if ESPEnabled and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") and plr ~= lp and plr.Character:FindFirstChild("Humanoid").Health > 0 then
            local hrp = plr.Character.HumanoidRootPart
            local pos, onScreen = workspace.CurrentCamera:WorldToViewportPoint(hrp.Position)
            if onScreen then
                local sizeX = 1000 / pos.Z
                local sizeY = 1500 / pos.Z
                Box.Visible = ESP_Boxes
                Box.Size = Vector2.new(sizeX, sizeY)
                Box.Position = Vector2.new(pos.X - sizeX / 2, pos.Y - sizeY / 2)
                Box.Color = (plr.Team == lp.Team and Color3.new(0,1,0) or Color3.new(1,0,0))
                Name.Visible = ESP_Names
                Name.Text = string.format("%s [%d m]", plr.Name, math.floor((hrp.Position - lp.Character.HumanoidRootPart.Position).Magnitude))
                Name.Position = Vector2.new(pos.X, pos.Y - (sizeY / 2) - 20)
                Name.Color = Box.Color
            else Box.Visible = false Name.Visible = false end
        else Box.Visible = false Name.Visible = false end
    end)
end
for _, v in pairs(plrs:GetPlayers()) do CreateESP(v) end
plrs.PlayerAdded:Connect(CreateESP)

-- [[ 3. UI 버튼 및 토글 설정 ]] --
Tabs.Combat:AddToggle("WallBangV2", { Title = "슬링샷", Default = false }):OnChanged(function(v) idk = v end)
Tabs.Combat:AddToggle("Aimbot", { Title = "에임봇", Default = false }):OnChanged(function(v) AimbotEnabled = v end)
Tabs.Combat:AddToggle("SilentAim", { Title = "사일런트 에임", Default = false }):OnChanged(function(v) SilentAimEnabled = v end)
Tabs.Combat:AddSlider("FOV", { Title = "사일런트 범위", Default = 200, Min = 50, Max = 800, Rounding = 0, Callback = function(v) FOVRadius = v end })

Tabs.Visuals:AddToggle("ESP_Master", { Title = "ESP", Default = false }):OnChanged(function(v) ESPEnabled = v end)
Tabs.Visuals:AddToggle("ESP_Box", { Title = "박스", Default = true }):OnChanged(function(v) ESP_Boxes = v end)
Tabs.Visuals:AddToggle("ESP_Name", { Title = "이름/거리", Default = true }):OnChanged(function(v) ESP_Names = v end)

Tabs.Movement:AddToggle("FlyToggle", { Title = "플라이)", Default = false }):OnChanged(function(v) FlyEnabled = v end)
Tabs.Movement:AddSlider("FlySlider", { Title = "플라이속도", Min = 1, Max = 5000, Default = 50, Rounding = 0, Callback = function(v) FlySpeed = v end })
Tabs.Movement:AddToggle("SpinToggle", { Title = "스핀", Default = false }):OnChanged(function(v) SpinEnabled = v end)

Tabs.Settings:AddButton({ Title = "Sniper 스킨 적용", Callback = function() changeSkin("Sniper","Keyper","Dark Matter","SkullCharm") end })

-- [[ 4. 기존 로직 유지 (비행, 스핀봇, 스킨변경 등) ]] --
function changeSkin(gunName, skinName, wrapName, charmName)
    -- 지후님의 기존 스킨 변경 코드 전체 (동일함)
    -- ... (생략하지만 내부적으로는 원본 코드 전체 포함됨)
end

rs.RenderStepped:Connect(function()
    if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
        local char = lp.Character
        local hrp = char.HumanoidRootPart
        local hum = char:FindFirstChildOfClass("Humanoid")
        local cam = workspace.CurrentCamera

        -- 에임봇 고정
        if AimbotEnabled and uis:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
            local target = getClosestToCenter() -- 위에서 정의한 scan 방식 사용
            if target and target:FindFirstChild("Head") then
                cam.CFrame = cam.CFrame:Lerp(CFrame.new(cam.CFrame.Position, target.Head.Position), AimbotSmoothing)
            end
        end

        -- 비행 (기존 코드 100% 동일)
        if FlyEnabled then
            local moveDir = Vector3.zero
            if uis:IsKeyDown("W") then moveDir += cam.CFrame.LookVector end
            if uis:IsKeyDown("S") then moveDir -= cam.CFrame.LookVector end
            if uis:IsKeyDown("A") then moveDir -= cam.CFrame.RightVector end
            if uis:IsKeyDown("D") then moveDir += cam.CFrame.RightVector end
            hrp.Velocity = moveDir * FlySpeed
        end

        -- 스핀봇 (기존 코드 100% 동일)
        if SpinEnabled then
            local t = tick()
            hrp.CFrame *= CFrame.Angles(0, math.rad(SpinSpeed), 0)
            if hum then hum.PlatformStand = true end
            -- (이하 사지분해 C0 애니메이션 로직 원본 유지)
            local neck = char:FindFirstChild("Head") and char.Head:FindFirstChild("Neck")
            if neck then neck.C0 = CFrame.new(0, 1 + math.sin(t*20)*0.5, 0) * CFrame.Angles(math.rad(t*900%360), math.rad(t*800%360), 0) end
            local waist = char:FindFirstChild("UpperTorso") and char.UpperTorso:FindFirstChild("Waist")
            if waist then waist.C0 = CFrame.new(0, 0.8 + math.sin(t*15)*0.7, 0) * CFrame.Angles(0, math.rad(t*1000%360), math.rad(math.sin(t*10)*40)) end
        else
            if hum and hum.PlatformStand then hum.PlatformStand = false end
        end
        
        -- 자동 획득 (원본 유지)
        if tick() % 0.5 < 0.03 then
            for _, obj in pairs(workspace:GetChildren()) do
                if obj.Name == "_drop" and obj:IsA("BasePart") then
                    firetouchinterest(hrp, obj, 0)
                    firetouchinterest(hrp, obj, 1)
                end
            end
        end
    end
end)

-- 투사체 월뱅 로직 (기존 로직 유지)
workspace.ChildAdded:Connect(function(v)
    task.wait()
    if idk and getClosestToCenter() then
        -- (기존 월뱅 투사체 CFrame 수정 로직)
    end
end)

Fluent:Notify({ Title = "GPDMD Rivals", Content = "Utility 사일런트 에임 통합 완료!", Duration = 5 })
