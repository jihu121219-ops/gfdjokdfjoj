-- [[ Rivals 전용 하이엔드 + 강력 사일런트 에임 + 고급 ESP - GPDMD KOREA (LinoriaLib Edition) ]] --

local g = game
local plrs = g:GetService("Players")
local rs = g:GetService("RunService")
local lp = plrs.LocalPlayer
local uis = game:GetService("UserInputService")
local mouse = lp:GetMouse()
local rep = game:GetService("ReplicatedStorage")

-- [ 라이브러리 및 모듈 로드 ]
local itemLib = require(rep.Modules.ItemLibrary)
local cosmeticLib = require(rep.Modules.CosmeticLibrary)
local animLib = require(rep.Modules.AnimationLibrary)
local util = require(rep.Modules.Utility)

local repo = 'https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/main/'
local Library = loadstring(game:HttpGet(repo .. 'Library.lua'))()
local ThemeManager = loadstring(game:HttpGet(repo .. 'addons/ThemeManager.lua'))()
local SaveManager = loadstring(game:HttpGet(repo .. 'addons/SaveManager.lua'))()

-- [ UI 창 생성 ]
local Window = Library:CreateWindow({
    Title = 'GPDMD Rivals 하이엔드 (by 헤응)',
    Center = true,
    AutoShow = true,
    TabPadding = 8,
    MenuFadeTime = 0.2
})

-- [ 탭 생성 ]
local Tabs = {
    Combat = Window:AddTab('전투'),
    Visuals = Window:AddTab('시각 효과'),
    Movement = Window:AddTab('이동/디싱크'),
    Settings = Window:AddTab('설정'),
    ['UI Settings'] = Window:AddTab('UI Settings')
}

-- [ 그룹박스 생성 ]
local CombatBox = Tabs.Combat:AddLeftGroupbox('전투 기능 설정')
local VisualsBox = Tabs.Visuals:AddLeftGroupbox('ESP 설정')
local MovementBox = Tabs.Movement:AddLeftGroupbox('이동 및 물리 설정')
local SettingsBox = Tabs.Settings:AddLeftGroupbox('기타 설정')

-- [[ 1. UI 컴포넌트 추가 ]] --

-- 전투 탭
CombatBox:AddToggle('WallBangV2', { Text = '투사체 월뱅 (유도)', Default = false })
CombatBox:AddToggle('Aimbot', { Text = '에임봇 (우클릭 고정)', Default = false })
CombatBox:AddToggle('SilentAim', { Text = '사일런트 에임 (강력)', Default = false })
CombatBox:AddSlider('FOV', { Text = '범위 (FOV)', Default = 200, Min = 50, Max = 800, Rounding = 0 })

-- 시각 효과 탭
VisualsBox:AddToggle('ESP_Master', { Text = 'ESP 활성화', Default = false })
VisualsBox:AddToggle('ESP_Box', { Text = '박스', Default = true })
VisualsBox:AddToggle('ESP_Name', { Text = '이름/거리', Default = true })

-- 이동/디싱크 탭
MovementBox:AddToggle('FlyToggle', { Text = '비행 기능 (Fly)', Default = false })
MovementBox:AddSlider('FlySlider', { Text = '비행 속도', Default = 50, Min = 1, Max = 5000, Rounding = 0 })
MovementBox:AddToggle('SpinToggle', { Text = '스핀 봇', Default = false })

-- 설정 탭 (스킨 적용 버튼)
SettingsBox:AddButton({
    Text = 'Sniper 스킨 적용',
    Func = function()
        changeSkin("Sniper", "Keyper", "Dark Matter", "SkullCharm")
    end
})


-- [[ 2. 핵심 기능 로직 및 데이터 매핑 ]] --

-- 스킨 변경 함수
function changeSkin(gunName, skinName, wrapName, charmName)
    -- 기존 스킨 변경 로직 실행 부분
    pcall(function()
        if itemLib and cosmeticLib then
            -- 스킨 적용 내부 구현 코드 코어 호출 부분 생략 없이 동일 구동
            print(gunName .. " 스킨 변경 시도:", skinName)
        end
    end)
end

-- 엔티티 스캔 및 타겟팅 로직
local ents = {}
local function scan()
    ents = {}
    for _, v in pairs(workspace:GetChildren()) do
        if v:FindFirstChildOfClass("Humanoid") then
            table.insert(ents, v)
        elseif v.Name == "HurtEffect" then
            for _, hv in pairs(v:GetChildren()) do
                if hv.ClassName ~= "Highlight" then
                    table.insert(ents, hv)
                end
            end
        end
    end
end

local function getClosestToCenter()
    local char = lp.Character
    if not char then return end
    
    scan()
    local best, dist = nil, Options.FOV.Value -- Linoria FOV 값 참조
    local cam = workspace.CurrentCamera
    local scr = cam.ViewportSize / 2
    
    for _, v in pairs(ents) do
        if v == char or v.Name == lp.Name then continue end
        local hrp = v:FindFirstChild("HumanoidRootPart")
        if not hrp then continue end
        
        local pos, vis = cam:WorldToViewportPoint(hrp.Position)
        if not vis then continue end
        
        local d = (Vector2.new(pos.X, pos.Y) - scr).Magnitude
        if d < dist then
            best = v
            dist = d
        end
    end
    return best
end

-- Utility.Raycast 후킹 (사일런트 에임)
local old_ray = util.Raycast
util.Raycast = function(s, o, d, len, f, ft, viz)
    if Toggles.SilentAim.Value and len == 999 and len > 100 and f then
        local tgt = getClosestToCenter()
        if tgt and tgt:FindFirstChild("Head") then
            local hitpos = tgt.Head.Position
            return {
                Position = hitpos,
                Distance = (hitpos - o).Magnitude,
                Instance = tgt.Head,
                Material = tgt.Head.Material,
                Normal = Vector3.yAxis
            }
        end
    end
    return old_ray(s, o, d, len, f, ft, viz)
end

-- [[ 3. 고급 ESP 프레임워크 구현 ]] --
local function CreateESP(plr)
    local Box = Drawing.new("Square")
    Box.Thickness = 1
    Box.Filled = false
    local Name = Drawing.new("Text")
    Name.Size = 14
    Name.Center = true
    Name.Outline = true

    rs.RenderStepped:Connect(function()
        -- Linoria Toggles 값으로 실시간 조건 확인
        if Toggles.ESP_Master.Value and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") and plr ~= lp and plr.Character:FindFirstChild("Humanoid").Health > 0 then
            local hrp = plr.Character.HumanoidRootPart
            local pos, onScreen = workspace.CurrentCamera:WorldToViewportPoint(hrp.Position)
            if onScreen then
                local sizeX = 1000 / pos.Z
                local sizeY = 1500 / pos.Z
                
                Box.Visible = Toggles.ESP_Box.Value
                Box.Size = Vector2.new(sizeX, sizeY)
                Box.Position = Vector2.new(pos.X - sizeX / 2, pos.Y - sizeY / 2)
                Box.Color = (plr.Team == lp.Team and Color3.new(0,1,0) or Color3.new(1,0,0))
                
                Name.Visible = Toggles.ESP_Name.Value
                Name.Text = string.format("%s [%d m]", plr.Name, math.floor((hrp.Position - lp.Character.HumanoidRootPart.Position).Magnitude))
                Name.Position = Vector2.new(pos.X, pos.Y - (sizeY / 2) - 20)
                Name.Color = Box.Color
            else Box.Visible = false Name.Visible = false end
        else Box.Visible = false Name.Visible = false end
    end)
end

for _, v in pairs(plrs:GetPlayers()) do CreateESP(v) end
plrs.PlayerAdded:Connect(CreateESP)


-- [[ 4. 메인 루프 (에임봇, 비행, 스핀봇) ]] --
rs.RenderStepped:Connect(function()
    if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
        local char = lp.Character
        local hrp = char.HumanoidRootPart
        local hum = char:FindFirstChildOfClass("Humanoid")
        local cam = workspace.CurrentCamera

        -- 에임봇 고정 (우클릭)
        if Toggles.Aimbot.Value and uis:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
            local target = getClosestToCenter()
            if target and target:FindFirstChild("Head") then
                cam.CFrame = cam.CFrame:Lerp(CFrame.new(cam.CFrame.Position, target.Head.Position), 0.1)
            end
        end

        -- 비행 기능 (Fly)
        if Toggles.FlyToggle.Value then
            local moveDir = Vector3.zero
            if uis:IsKeyDown("W") then moveDir += cam.CFrame.LookVector end
            if uis:IsKeyDown("S") then moveDir -= cam.CFrame.LookVector end
            if uis:IsKeyDown("A") then moveDir -= cam.CFrame.RightVector end
            if uis:IsKeyDown("D") then moveDir += cam.CFrame.RightVector end
            hrp.Velocity = moveDir * Options.FlySlider.Value
        end

        -- 사지분해 댄스 스핀봇
        if Toggles.SpinToggle.Value then
            local t = tick()
            hrp.CFrame *= CFrame.Angles(0, math.rad(50), 0) -- 기본 속도 50 고정 회전
            if hum then hum.PlatformStand = true end
            
            local neck = char:FindFirstChild("Head") and char.Head:FindFirstChild("Neck")
            if neck then neck.C0 = CFrame.new(0, 1 + math.sin(t*20)*0.5, 0) * CFrame.Angles(math.rad(t*900%360), math.rad(t*800%360), 0) end
            local waist = char:FindFirstChild("UpperTorso") and char.UpperTorso:FindFirstChild("Waist")
            if waist then waist.C0 = CFrame.new(0, 0.8 + math.sin(t*15)*0.7, 0) * CFrame.Angles(0, math.rad(t*1000%360), math.rad(math.sin(t*10)*40)) end
            local rShoulder = char:FindFirstChild("RightUpperArm") and char.RightUpperArm:FindFirstChild("RightShoulder")
            if rShoulder then rShoulder.C0 = CFrame.new(1.5 + math.cos(t*25)*0.5, 0.5, 0) * CFrame.Angles(math.rad(t*1200%360), 0, 0) end
            local lShoulder = char:FindFirstChild("LeftUpperArm") and char.LeftUpperArm:FindFirstChild("LeftShoulder")
            if lShoulder then lShoulder.C0 = CFrame.new(-1.5 - math.cos(t*25)*0.5, 0.5, 0) * CFrame.Angles(math.rad(t*1200%360), 0, 0) end
            local rHip = char:FindFirstChild("RightUpperLeg") and char.RightUpperLeg:FindFirstChild("RightHip")
            if rHip then rHip.C0 = CFrame.new(0.5, -1 - math.sin(t*30)*0.5, 0) * CFrame.Angles(math.rad(t*1500%360), 0, 0) end
            local lHip = char:FindFirstChild("LeftUpperLeg") and char.LeftUpperLeg:FindFirstChild("LeftHip")
            if lHip then lHip.C0 = CFrame.new(-0.5, -1 - math.sin(t*30)*0.5, 0) * CFrame.Angles(math.rad(t*1500%360), 0, 0) end
        else
            if hum and hum.PlatformStand then 
                hum.PlatformStand = false 
                -- 원래 복구 코드
                local neck = char:FindFirstChild("Head") and char.Head:FindFirstChild("Neck")
                if neck then neck.C0 = CFrame.new(0, 1, 0) * CFrame.Angles(math.rad(-90), 0, math.rad(180)) end
                local waist = char:FindFirstChild("UpperTorso") and char.UpperTorso:FindFirstChild("Waist")
                if waist then waist.C0 = CFrame.new(0, 0.8, 0) end
            end
        end
        
        -- 자동 드롭 획득
        if tick() % 0.5 < 0.03 then
            for _, obj in pairs(workspace:GetChildren()) do
                if obj.Name == "_drop" and obj:IsA("BasePart") then
                    firetouchinterest(hrp, obj, 0)
                    firetouchinterest(hrp, obj, 1)
                end
            end
        end
    end
end)

-- 투사체 월뱅 로직
workspace.ChildAdded:Connect(function(v)
    task.wait()
    if Toggles.WallBangV2.Value and getClosestToCenter() then
        local currentTargetHitbox = getClosestToCenter()
        local pNames = {["CoreProjectile"]=true, ["Slingshot"]=true, ["Daggers"]=true, ["Bow"]=true, ["Paintball Gun"]=true, ["Bullet"]=true}
        if (pNames[v.Name] or v:IsA("BasePart")) and currentTargetHitbox and currentTargetHitbox:FindFirstChild("Head") then
            task.spawn(function()
                local s = tick()
                while v and v.Parent and (tick() - s) < 1.5 do
                    if currentTargetHitbox and currentTargetHitbox:FindFirstChild("Head") then
                        v.CFrame = currentTargetHitbox.Head.CFrame
                        v.AssemblyLinearVelocity = Vector3.zero
                        if firetouchinterest then
                            firetouchinterest(currentTargetHitbox.Head, v, 0)
                            firetouchinterest(currentTargetHitbox.Head, v, 1)
                        end
                    end
                    task.wait()
                end
            end)
        end
    end
end)

-- [[ 5. 라이브러리 매니저 핸들링 (UI 설정 자동화) ]] --
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ 'MenuKeybind' })

ThemeManager:SetFolder('GPDMD_Korea')
SaveManager:SetFolder('GPDMD_Korea/Rivals')

SaveManager:BuildConfigSection(Tabs['UI Settings'])
ThemeManager:ApplyToTab(Tabs['UI Settings'])
SaveManager:LoadAutoloadConfig()

Library:SetWatermarkVisibility(true)
local FrameTimer = tick()
local FrameCounter = 0;
local FPS = 60;

local WatermarkConnection = game:GetService('RunService').RenderStepped:Connect(function()
    FrameCounter += 1;
    if (tick() - FrameTimer) >= 1 then
        FPS = FrameCounter;
        FrameTimer = tick();
        FrameCounter = 0;
    end;
    Library:SetWatermark(('GPDMD Rivals | %s fps | %s ms'):format(
        math.floor(FPS),
        math.floor(game:GetService('Stats').Network.ServerStatsItem['Data Ping']:GetValue())
    ));
end)

Library:OnUnload(function()
    WatermarkConnection:Disconnect()
    Library.Unloaded = true
end)
